let myName: string = 'Alfred';
let myAge: number = 25;
let anything;
anything = 12;

function controlMe(isTrue: boolean) {
    let result: number;
    if (isTrue = true) {
        result: 12;
    }
    return result;
}